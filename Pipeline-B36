def hello = "Welcome to Pipeline"
pipeline {
    
    agent {
        label 'BuildServer'
    }

    stages {
        stage('Hello') {
            
            steps {
                echo "${hello}"
            }
        }
    }
}
